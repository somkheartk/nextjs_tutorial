# Next.js Tutorial ภาษาไทย

เว็บไซต์สอนการใช้งาน Next.js ภาษาไทย พร้อมตัวอย่างโค้ดและคำอธิบายอย่างละเอียด

## 🚀 เริ่มต้นเรียนรู้

คลิกที่ลิงก์ด้านล่างเพื่อเข้าสู่บทเรียน หรือรันโปรเจคนี้ในเครื่องของคุณแล้วเปิด `http://localhost:3000`

## คุณสมบัติ

- 📚 บทเรียนภาษาไทยที่เข้าใจง่าย
- 💻 ตัวอย่างโค้ดพร้อมใช้งาน พร้อมปุ่มคัดลอก
- 🎨 ออกแบบสไตล์คล้ายกับ nextjs.org
- 🌙 รองรับ Dark Mode
- 📱 Responsive Design
- 🔗 Navigation ที่ใช้งานง่าย

## 📚 บทเรียนทั้งหมด (คลิกเพื่ออ่าน)

### บทที่ 1: [พื้นฐานที่ต้องรู้](/docs/basics)
> เรียนรู้พื้นฐาน React, Components, Props, State และ JSX ก่อนเริ่มต้นใช้งาน Next.js

**หัวข้อที่จะได้เรียนรู้:**
- React คืออะไร?
- Component คืออะไร? (Function Component vs Class Component)
- JSX และกฎการเขียน
- Props - การส่งข้อมูลระหว่าง Components
- State - การจัดการสถานะด้วย useState
- useEffect - การจัดการ Side Effects
- Server Components vs Client Components

**ตัวอย่างโค้ด:**
```tsx
// ตัวอย่าง Function Component
function Welcome({ name }: { name: string }) {
  return <h1>สวัสดี, {name}!</h1>;
}

// ตัวอย่าง State
const [count, setCount] = useState(0);
```

---

### บทที่ 2: [เริ่มต้นใช้งาน](/docs/getting-started)
> การติดตั้งและตั้งค่าโปรเจค Next.js ใหม่

**หัวข้อที่จะได้เรียนรู้:**
- ความต้องการของระบบ (Node.js 18.17+)
- การติดตั้ง Next.js ด้วย create-next-app
- โครงสร้างโปรเจค Next.js
- การรัน Development Server
- การแก้ไขหน้าแรก

**ตัวอย่างโค้ด:**
```bash
# สร้างโปรเจคใหม่
npx create-next-app@latest my-app

# รัน development server
cd my-app
npm run dev
```

---

### บทที่ 3: [การกำหนดเส้นทาง (Routing)](/docs/routing)
> ระบบ Routing ด้วย App Router แบบ File-based

**หัวข้อที่จะได้เรียนรู้:**
- File-based Routing
- การสร้างหน้าใหม่
- Dynamic Routes (`[param]`)
- Catch-all Routes (`[...slug]`)
- การนำทางด้วย Link Component
- useRouter Hook

**ตัวอย่างโค้ด:**
```tsx
// Dynamic Route: app/blog/[slug]/page.tsx
export default async function BlogPost({ params }: Props) {
  const { slug } = await params;
  return <h1>บทความ: {slug}</h1>;
}
```

---

### บทที่ 4: [การดึงข้อมูล (Data Fetching)](/docs/data-fetching)
> Server Components และ Client Components สำหรับการดึงข้อมูล

**หัวข้อที่จะได้เรียนรู้:**
- Server Components (แนะนำ)
- การแคชข้อมูล (Caching)
- Client Components กับ useEffect
- SWR Library
- Server Actions

**ตัวอย่างโค้ด:**
```tsx
// Server Component - ดึงข้อมูลบน Server
async function getPosts() {
  const res = await fetch('https://api.example.com/posts');
  return res.json();
}

export default async function PostsPage() {
  const posts = await getPosts();
  return <ul>{posts.map(post => <li key={post.id}>{post.title}</li>)}</ul>;
}
```

---

### บทที่ 5: [API Routes](/docs/api-routes)
> การสร้าง API Endpoints ด้วย Route Handlers

**หัวข้อที่จะได้เรียนรู้:**
- การสร้าง Route Handler
- GET, POST, PUT, DELETE requests
- Dynamic API Routes
- Query Parameters
- Headers และ Cookies
- การเรียก API จาก Client

**ตัวอย่างโค้ด:**
```typescript
// app/api/users/route.ts
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'สวัสดี!' });
}

export async function POST(request: Request) {
  const body = await request.json();
  return NextResponse.json({ user: body }, { status: 201 });
}
```

---

### บทที่ 6: [การจัดสไตล์ (Styling)](/docs/styling)
> CSS, Tailwind CSS และ CSS-in-JS

**หัวข้อที่จะได้เรียนรู้:**
- Global CSS
- CSS Modules
- Tailwind CSS (แนะนำ)
- CSS-in-JS (styled-components)
- Dark Mode
- Responsive Design

**ตัวอย่างโค้ด:**
```tsx
// Tailwind CSS
<div className="rounded-lg border border-zinc-200 bg-white p-6 
  dark:border-zinc-800 dark:bg-zinc-900">
  <h2 className="text-xl font-bold text-zinc-900 dark:text-white">
    รองรับ Dark Mode
  </h2>
</div>
```

---

## 🛠️ การติดตั้งและรัน

```bash
# Clone โปรเจค
git clone https://github.com/somkheartk/nextjs_tutorial.git
cd nextjs_tutorial

# ติดตั้ง dependencies
npm install

# รัน development server
npm run dev

# Build สำหรับ production
npm run build

# รัน production server
npm run start
```

เมื่อรันแล้ว เปิดเบราว์เซอร์ไปที่ `http://localhost:3000` เพื่อดูเว็บไซต์

## 📁 โครงสร้างโปรเจค

```
nextjs_tutorial/
├── src/
│   ├── app/
│   │   ├── docs/                 # บทเรียนทั้งหมด
│   │   │   ├── basics/           # บทที่ 1: พื้นฐาน
│   │   │   ├── getting-started/  # บทที่ 2: เริ่มต้นใช้งาน
│   │   │   ├── routing/          # บทที่ 3: Routing
│   │   │   ├── data-fetching/    # บทที่ 4: Data Fetching
│   │   │   ├── api-routes/       # บทที่ 5: API Routes
│   │   │   └── styling/          # บทที่ 6: Styling
│   │   ├── layout.tsx
│   │   ├── page.tsx              # หน้าแรก
│   │   └── globals.css
│   └── components/
│       ├── CodeBlock.tsx         # แสดงโค้ดพร้อมปุ่มคัดลอก
│       ├── Navbar.tsx            # แถบนำทางด้านบน
│       └── Sidebar.tsx           # เมนูด้านข้าง
├── public/
├── package.json
└── README.md
```

## 🔧 เทคโนโลยีที่ใช้

| เทคโนโลยี | เวอร์ชัน | รายละเอียด |
|----------|---------|-----------|
| [Next.js](https://nextjs.org/) | 16.0.7 | React Framework |
| [React](https://react.dev/) | 19.2.0 | UI Library |
| [TypeScript](https://www.typescriptlang.org/) | ^5 | Type Safety |
| [Tailwind CSS](https://tailwindcss.com/) | ^4 | Utility-first CSS |

## 📖 ลำดับการเรียนรู้ที่แนะนำ

1. **ผู้เริ่มต้น:** เริ่มจาก [พื้นฐานที่ต้องรู้](/docs/basics) → [เริ่มต้นใช้งาน](/docs/getting-started)
2. **มีพื้นฐานแล้ว:** [การกำหนดเส้นทาง](/docs/routing) → [การดึงข้อมูล](/docs/data-fetching)
3. **ขั้นสูง:** [API Routes](/docs/api-routes) → [การจัดสไตล์](/docs/styling)

## 🤝 การมีส่วนร่วม

ยินดีต้อนรับการมีส่วนร่วมในการพัฒนา! สามารถ:
- 🐛 เปิด Issue แจ้งปัญหาที่พบ
- 💡 เสนอแนะเนื้อหาใหม่
- 🔧 ส่ง Pull Request เพื่อแก้ไขหรือเพิ่มเติม

## 📝 License

MIT License - สามารถนำไปใช้ แก้ไข และแจกจ่ายได้อย่างอิสระ
